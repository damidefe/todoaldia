[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python manage.py collectstatic --noinput"
]

[start]
cmd = "python manage.py migrate && gunicorn todoaldia.wsgi"